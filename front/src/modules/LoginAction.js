import { createAction } from 'redux-actions';

export const loginRequest = createAction('LOGIN_REQUEST');
export const loginRequest = createAction('LOGIN_SUCCESS');
export const loginRequest = createAction('LOGIN_FAIL');